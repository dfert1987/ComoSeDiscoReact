import React, {} from 'react';
import { Nav } from '../../Components/Nav';


export const Home = () => {
    return(
         <div>
            <Nav id={'nav'} navItems={'navItems'}/>
        </div>
    );
};