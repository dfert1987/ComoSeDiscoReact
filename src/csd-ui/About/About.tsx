import React, {} from 'react';
import { Nav } from '../../Components/Nav';

export const About = () => {
    return(
         <div>
            <Nav id={'nav'} navItems={'navItems'}/>
        </div>
    );
};